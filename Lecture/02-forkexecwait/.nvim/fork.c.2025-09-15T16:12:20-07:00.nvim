#include <stdio.h>
#include <unistd.h>

int main() {
  pid_t pid = getpid();
  fork();
  pid_t ppid = getppid();
  if (pid == ppid) {
    printf("This is Child \n");
  } else {
    printf("This is Parent \n");
  }
}
