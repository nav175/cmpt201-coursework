#include <stdio.h>
#include <string.h>
#include <unistd.h>

int main() {
  char *input = NULL;
  int size = 0;
  printf("Enter the path to the program: ");
  getline(&input,&size, stdin);
  printf("User entered %c\n", input);
  return 0;
}
